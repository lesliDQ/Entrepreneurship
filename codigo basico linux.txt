ls = muestra los archivos que se estè guardando en ese archivo
ls -la = Muestra el historial + fecha de instalaciòn con hora
mkdir = crear un archivo
touch = crear archivos
rm -rf = para eliminar carpetas
rm = para eliminar archivos
history > = para saber el historial de tareas desde el instalador

git init = Se creo un repositorio donde identifica el servicio local como master=
git status = Que es lo que se encuentra en mi directorio de carpetas 
git add . = Es para llevar al stage
git status = para saber que se tiene que confirmar
git commit -m " Nombre de la moficicafion" = Sirve para confirmar la modificaciòn
git log = Para conocer las modificaciones que se han realizado (detalle)
git tag "Nombre de la etiqueta" = Para poner nombre de eqiqueta al cambio
git tag -l = muestra el nombre de la modificaciones tras ser verificadas
git branch ¨Nombre de la rama creada"
git branch -l = conocer todas las ramas que hay y conocer en donde se esta trabajando
git checkout ¨Nombre de la rama que se quiere trabajar" 